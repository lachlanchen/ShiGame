# Engine and workstation status

Last checked: 2026-08-11, Asia/Hong_Kong.

## Selected engines

Unreal Engine 5.8 is now the priority cinematic/3D client by explicit product decision. The Web client remains the playable reference and first-class shipping client. Unity 6 LTS (`6000.0.80f1`, `2dfd32957da2`) remains a maintained shared-schema baseline; it is not the feature-first 3D track.

## Installed and verified

- Node.js 22.21, npm, Git, GitHub CLI.
- Blender 4.0.2; headless render succeeded.
- AgenticApp/LabCanvas scene renderer; PNG and `.blend` generation succeeded.
- Blender GLB and FBX export; GLB re-import succeeded with 19 meshes.
- Xvfb, x11vnc, websockify/noVNC and Chrome/CDP visible playtest stack.
- Epic official Unreal Engine `5.8.1` installed build (changelist `56057345`); archive integrity, staged extraction and atomic outside-Git installation passed.
- Unreal project-file generation, runtime/editor-module compilation/linking with bundled Clang 20.1.8, and the exact twenty-two-suite `SHI.` native automation run; all twenty-two suites pass with exit code 0, including five identity-Root skeletal characters, two shared-skeleton body-performance clips, the exact 21-control silent facial-intent contract, the isolated Chen Sheng skin-material route, the Gate 5A wet-register interaction contract, command-weight/surface/field/shelter/rain/vegetation presentation, 76-route Broken Crossing parity and bounded six-piece spatial contracts.
- Stable visible fixed-window Unreal standalone route on the localhost-only `:121` noVNC desktop: canonical council, three tactical pulses, separate field answers, terminal outcome, six simultaneous 3D tallies and exact campaign-save preservation all observed.
- Official UE 5.8.1 Linux development package: BuildCookRun exits 0, cooks 496 packages, archives a 1,099,965,122-byte outside-Git player, and preserves byte-identical campaign and engagement StreamingAssets.
- Normal-thread packaged Vulkan player: complete Broken Crossing route and exact save isolation observed. A real 60 Hz display FPS chart records 195.18 FPS average with zero hitches and zero missed 30/60/120-FPS targets; Xvfb/noVNC remains capture/presentation-limited and is not used as the product performance result.
- Packaged failure acceptance: unwritable save, missing council figures and missing command-signal actors all visibly hold the order/world/chronicle and preserve the exact save; a restart restores actors.
- Packaged reduced-motion and native-audio measurements: cuts-only consequence/skip/handoff and restart persistence pass; direct SDL/PipeWire capture proves digital silence before consent/after disable and bounded non-silent output after opt-in.
- Command-space and cast blockouts: the exact command weight, fictional earth/wood surface, `24 × 24 m` wet field, open Daze field shelter, deterministic rain, restrained 106-instance field-edge vegetation, five shared-skeleton council identities and two body-performance loops pass their bounded source/import/runtime checks. The isolated facial lane admits five morph-bearing meshes, fifteen materials and one CC0 eye texture; source/FBX/GLB equivalence, read-only Unreal inspection, native compilation and the final 20/20 `SHI.` run pass. Its accepted v2 Linux package mounts 559 packages. Watched 1600×1000 noVNC review accepts Chen Sheng as normal-motion speaker, the keeper as normal-motion listener and Chen Sheng with `ReducedMotion=True`, while retaining the generic face as an engineering blockout. Final character art, close framing, mouth interior, voice/lip sync, visible fail-closed fallback and human art/historical review remain rejected.
- Chen Sheng skin-material engineering: three imported textures, one Subsurface Profile and one isolated material pass corrected Opacity/SSS semantics, privacy-v11 serialized-metadata checks, read-only import stability, native compilation and the final 21/21 `SHI.` run. The path-mapped, RPATH-sanitized Development package cooks 564 packages and the sequential normal/reduced watched routes remain story/save inert. This admits the technical material route only; the generic low-detail visual character result, identity, anatomy, close framing, final skin and human review remain rejected.
- Gate 5A wet-register interaction engineering: the pinned source suite passes **91 / 91** with ordered-release onset at sample `90` / `3.000 s` and first measured physical exit at sample `91`; native build v7 succeeds across **41 / 41** actions; focused automation v5 passes **1 / 1** and full automation v5 passes the complete **22 / 22** `SHI.` namespace. Import v7/inspect-only v2 admits exactly one texture-free material, one `32 × 14 × 2 cm` prop and one 121-sample animation on the accepted 53-bone skeleton. Detached clean-source package v4 completes **567 / 0 / 7 / 574** packages cooked / incrementally skipped / platform-skipped / total and passes StreamingAssets, nine-entry relative-RPATH, dependency and privacy inspection. Sequential normal-v5/reduced-v5 runs pass exact marker order, save inertness and controlled shutdown. This is package/runtime engineering admission only: normal sample-`90` onset retains contact and its later separation frame is not exact sample-`91` evidence; reduced pose `91` passes the wrist-marker exit while visible hand-mesh clearance remains effectively illegible at full framing. Visible contact/release, anatomy/acting, final prop/material, ownership handoff, human review, accessibility/localization, close framing, mouth, voice/lip sync and final art stay red.
- Unity experimental CLI `0.1.0-beta.3` in the user environment.
- Official Unity Hub AppImage at `~/.local/share/unityhub/UnityHub.AppImage`.
- Unity `2022.3.62f3c1` compatibility editor at `~/Unity/Hub/Editor/2022.3.62f3c1`, registered in Hub with Linux and Web build support plus offline documentation.
- Dedicated Unity desktop: display `:123`, VNC `127.0.0.1:5934`, noVNC `http://127.0.0.1:6134/vnc.html?host=127.0.0.1&port=6134&autoconnect=1&resize=scale`.
- Verified compatibility editor archive: 4,153,503,732 bytes; XZ integrity passed; SHA-256 `ee060f7d5f4753db2154eccb549ef283a08bfa36069178274165074d333f47c1`.

## Open blockers

### Unreal visible-play, package and remaining runtime proof

The official installed build now compiles the current Unreal runtime plus the editor-only animation normalizer and executes project automation. Project generation uses Epic's Linux batch script; targets use Unreal 5.8's `BuildSettingsVersion.V7`; bundled Clang 20.1.8 compiles and links both modules. The exact `SHI.` namespace discovers twenty-two—not similarly named engine—suites. Audio, all 46 campaign replays, immutable order transaction, save integrity, schema horizon, canonical council staging, identity-Root characters, shared-skeleton body performance, the exact deterministic 21-control silent facial-intent cadence, the Chen Sheng skin-material and wet-register interaction contracts, command-weight/surface/wet-field/shelter/rain/vegetation presentation, consequence grammar, nine campaign signals, 76-route Broken Crossing parity with six bounded spatial tallies, historical source/claim closure and wartable spatial intelligence all pass with exit code 0. Tests use `-nowrite`; intentional tracked configuration force-cooks each admitted authored-asset path.

The earlier council-performance archive mounts 538 packages and passes headless Entry/`ShiGameMode` smoke. Visible v5 review rejects the former 10000x Skeleton Root failure and preserves the complete animated body across measured-emphasis and settle phases. A separate normal run accepts real `1`/`Enter`/`Space` input, moves `rain-order → open-council`, changes resources, records one decision and autosaves. The later isolated facial engineering lane imports all five exact identities against the same 53-bone Skeleton, retains exactly 21 morphs per mesh, preserves the accepted neutral fallback and passes a no-op inspect-only hash check. Its first packaged visual pass was rejected: `SkinClay` and `EyeBrown` lacked Unreal's `MorphTargets` material usage, so the runtime warned that **Default Material** would be used. The corrected import saves all fifteen facial materials with `SkeletalMesh` usage and saves `MorphTargets` usage on exactly the two morph-bearing sections, `SkinClay` and `EyeBrown`, with no extra shader permutations.

The accepted facial v2 package at `$SHI_UNREAL_PACKAGE_ROOT/Linux` mounts 559 packages. One localhost-only noVNC stack was reused for three isolated watched reviews: Chen Sheng speaker with normal motion, keeper listener with normal motion and Chen Sheng speaker with `ReducedMotion=True`. Exact runtime markers were `SHI_COUNCIL_FACIAL_MORPH_SECTIONS_EXERCISED character=chen-sheng role=speaker state=object-glance skin=SkinClay eye=EyeBrown alpha=0.0624`, the same speaker marker at `alpha=1.0000` under reduced motion, and `SHI_COUNCIL_FACIAL_MORPH_SECTIONS_EXERCISED character=keeper role=listener state=object-glance skin=SkinClay eye=EyeBrown alpha=0.1218`. Six accepted 1600×1000 captures record speaker silent speech/blink, keeper listening/blink and reduced-motion object glance/terminal neutral. The final native `SHI.` namespace passes 20/20 with exit code 0. This accepts deterministic packaged facial presentation as an engineering blockout; it does not approve final character art, close framing, mouth interior, voice/lip sync, visible fail-closed fallback behavior or human art/historical performance.

The isolated Chen Sheng skin review adds exactly five assets over that facial package and cooks 564 packages. Its first shader route was rejected because the mask drove Subsurface Color rather than Subsurface Profile Opacity. The corrected package records `MaterialMasks2K.B = 89/255`, effective mean-free-path distance `0.9336`, exact texture/material/profile bindings and no targeted fallback. The source-mapped build plus pinned `patchelf` pass removes current-workstation path/token markers from the Development executable/debug/symbol set while preserving cooked content and resolving all runtime dependencies. Fresh v6 normal and reduced runs each admit and exercise the skin section exactly once, create no campaign save and close through controlled return code 143; timing-bound raw captures match their tracked PNGs pixel-for-pixel. The material route is technically accepted, but the watched result remains rejected as final visual art because the generic low-detail non-portrait face, hands, hair, mouth and clothing are visibly unfinished.

The wet-register interaction clears its successor source, native, Unreal-import, package and runtime-marker engineering boundaries. Blender validation passes 91/91 and separates ordered-release onset at sample `90` / `3.000 s` from first measured physical exit at sample `91`. Native build v7 succeeds across 41/41 actions, focused automation v5 passes 1/1 and full automation v5 passes all 22 native `SHI.` suites. The explicit importer-only `0.01` FBX unit correction restores the frozen prop to `32 × 14 × 2 cm`; import v7/inspect-only v2 preserves exactly the prop, one muted-clay material and the four-second/121-sample animation on the unchanged 53-bone skeleton, plus the exact socket/material/collision/navigation/privacy contracts and every accepted facial/skin/skeleton hash.

Detached clean-source package v4 completes **567 / 0 / 7 / 574** packages cooked / incrementally skipped / platform-skipped / total with `BUILD SUCCESSFUL` and AutomationTool exit code `0`; IoStore carries all 567 cooked packages and all five canonical StreamingAssets payloads are byte-equal to source. The open-source `patchelf` sanitizer changes nine RPATH entries to `$ORIGIN`-relative paths; independent inspection confirms all nine, zero unresolved libraries and no private-path or credential marker at the retained package boundary. UAT later appended a fresh AndroidFileServer token to the disposable worktree configuration; the filtered token/security marker reached neither package nor retained logs, so the post-cook temporary tree is not itself global credential-free evidence.

Sequential normal-v5 and reduced-v5 runs on one SHI desktop each emit one ordered inert/admitted/bilateral/held/release/terminal sequence, create no campaign save or progress file, avoid wet-register fail/fallback/error severities and complete the six-marker controlled shutdown with return code `143`. Normal motion records release onset at semantic/pose sample `90`; its onset image retains contact and its later visibly separated hand was captured `0.545645 s` after the onset marker, so it is not exact sample-`91` evidence. Reduced motion evaluates pose sample `91` and proves wrist-marker contact exit, but the visible hand mesh remains touching/overlapping enough that clearance is subtle to effectively non-legible in the 1600×1000 full framing. Terminal frames remain clamped without a gross restart; rain accounts for whole-frame pixel change. See the [packaged presentation evidence](evidence/unreal-daze-council-wet-register-interaction-presentation-status.json).

Earlier import v4/v5 and import v6 remain rejected/superseded history. Package/runtime v1–v3 also remain rejected: v1 used a noncanonical host-layout-relative RPATH; v2 omitted canonical StreamingAssets and failed campaign load; v3 restored payload parity but crossed the live right-contact bound at `1.0535 cm` before phase change. Normal-v4 is retained only as a technically successful late-monitor diagnostic without the required phase captures. The current decision is a **package/runtime engineering accept, visible hand-performance reject**. Visible-mesh contact/release, hand anatomy/acting, final prop/material, a visibly exercised fallback, player-ownership continuity and Keeper-to-Chen handoff, human historical-material/cultural review, accessibility/localization review, close framing, mouth interior, voice/lip sync and final character art/acting all remain red.

The three-pulse exercise now passes both visible standalone and archived-package routes on display `:121`. The actual packaged player advanced through two durable campaign decisions, selected and issued three tactical orders, received three separate authored field answers, reached **Costly success · Crossing under pressure**, saw all six metric pieces together and returned to the canonical council. The packaged on-disk campaign save SHA-256 was exactly `e9cbb4c857005fc04ac3dfe63b735296566655e0675e12c8045e8ccede2f42e8` before the first pulse, after the outcome and after close. The first standalone camera composition hid five pieces behind Slate; visible review caught it and the framing was corrected before package acceptance.

The official UE 5.8.1 package command completed compile, cook, stage and archive with exit code 0 in 102.32 seconds. It cooked 496 packages into a 1,099,965,122-byte outside-Git archive. The canonical, staged and packaged campaign payloads are byte-identical at SHA-256 `a82ab2f28809b46fb780074cce8acb96a7eb0b7cd608868f03b1888b64acf1f0`; the corresponding engagement payloads are byte-identical at `e5e7d40076e456c4044ab92f04209095af13fa5ff9599a66601f030b15133b8c`. Exact artifact hashes and performance receipts are recorded in [`unreal-linux-package-status.json`](evidence/unreal-linux-package-status.json).

The isolated command-weight council rebuild separately cooks 499 packages and mounts all 499 in a clean headless smoke. The static mesh and bulk geometry remain unchanged; the two isolated material assets now contain reviewed production-blockout graphs. `FShiCommandWeightPresentationModel` fixes exact centimeter bounds, `20°` yaw, table contact, edge and pointer-target clearance, 44° safe-frame/readability and the noninteractive/engagement-hidden policy. The first `-18°`/bright-bronze visible pass was rejected; retained council/front/back frames show the corrected broad silhouette, dark rough bronze, cord eye, underside attachment and contact shadow. A normal visible package then accepted Return and reached Act II Scene 2 with `TRANSACTION VERIFIED`.

The next two admitted blocks replace both default-white surfaces. The fictional command ground cooks 502 packages and preserves exact site/signal contact through visible story and engagement continuity. The wet field then cooks 505 packages—exactly three more assets—mounts all 505 in a clean smoke, and uses fail-closed bounds/material/clearance plus collision/navigation-disabled runtime policy. A pale-sand/flat-teal pass and an over-dark no-adaptation experiment were rejected; the accepted cooler field remains subordinate to the firelit council. Normal packaged input reaches Act III, completes all three engagement pulses and returns to exactly the same canonical campaign resources, turn and decisions. This is accepted only as a production blockout.

The shelter block closes the empty-sky shelter defect without claiming archaeology. Deterministic Blender sources and clean GLB/FBX round trips preserve an `8.40 × 6.73 × 3.55 m` four-post, open-sided wood/reed/cord shelter with `3,948 / 460` triangles, dual UVs and zero collision. Unreal authors compile-clean `8 / 11 / 8`-node PBR graphs, keeps the identity-root structure noninteractive/non-navigable, and rejects unsupported attestation or premature final-art state. Its clean player cooks and mounts `509` packages—exactly four beyond the wet-field baseline. This remains accepted only as a fictional practical production blockout.

The rain block adds two fixed-size instanced pools: 384 crossed-ribbon streaks and 72 exposed-ground ripples bounded to the exact `24 × 24 m` field. The shelter footprint intercepts streaks at `Z = 340 cm`; no ground ripple may spawn below it. Two texture-free six-node translucent/unlit materials compile their instanced-static-mesh permutations and preserve depth testing. The first package visibly fell back to the default material, and an inspect-only importer later resaved assets; both passes were rejected, the material usage and no-op inspection were corrected, and the final source-matched package mounts `513` packages. Fresh pointer play advances Act I → Act III, reaches **Orderly crossing**, and returns to the exact `32 / 36 / 33 / 56 / 62` campaign state with its 3,060-byte save hash and timestamp unchanged. This is a dramatic production VFX blockout—not evidence of exact weather in 209 BCE or final environment art. Its then-open vegetation gate is closed below; period figures/formations, final sky/lighting/weather response and human review remain red.

The vegetation block adds 42 taller stalk clumps and 64 low flattened tufts through two HISM components and one 15-node texture-free, vertex-alpha-masked GPU-wind material. Exact central and diagonal-route exclusions keep the shelter/command work and compacted approach open; the actor has no tick, collision, overlap, navigation, gameplay, save or replication authority. The first archive was rejected because its native actor compiled while string-only asset paths left IoStore byte-identical to the rain baseline. An explicit isolated force-cook contract corrects that boundary: the accepted player mounts `516` packages, exactly three beyond rain, and a fresh noVNC route reaches Act III, completes **Orderly crossing** and returns with save hash/timestamp unchanged. These are generic rain-flattened field-edge forms, not an exact botanical reconstruction or final environment art. Period figures/formations, interaction hands, cloth/hair response, final sky/lighting and human review remain red.

Normal render and RHI threads are green for the packaged fixed-window route. The player recovered from one startup outdated-swapchain notification, remained stable through the complete engagement and preserved the save. Unreal's built-in physical-display chart collected 4,180 frames over 21.42 seconds at 195.18 FPS average, 2.73 ms GPU, 4.11 ms render thread and 1.62 ms game thread, with zero hitches and zero missed 30/60/120-FPS targets. The same package averaged 24.57 FPS under active Xvfb/noVNC and 30.43 FPS with VNC capture paused while GPU/game work remained near 2 ms, proving a virtual-presentation limit rather than a content or GPU bottleneck. noVNC remains the interaction/review route, not the product frame-rate benchmark.

The command-weight review desktop repeats that separation after placement. Its clean steady frame reports 48.03 ms frame / 47.48 ms draw, but only 2.10 ms game and 4.17 ms GPU, with 59 draws, 19.0K primitives, 1.56 GiB memory and 1.93 GiB VRAM. These are useful scene counters and remote-path evidence, not a replacement physical-display performance certification. Physical timing must be repeated after rain, vegetation, final figures/formations and lighting are present.

Four real packaged fault injections are now green. An unwritable SaveGames directory/file creates no temp file and shows **Order not issued · Autosave failed** without changing the exact save. The same write fault on armed restart shows **Restart not applied · Current chronicle preserved** and keeps the exact Turn 4 hash. Development-console removal of both `AShiCouncilFigure` actors or all generic command/site actors makes order preflight name the unavailable council `speaker` or `resource-grain` signal and hold chronicle/world before persistence. Each injected run preserves the expected save; restart restores the actors.

Cuts-only runtime acceptance is green for the observed order. `V` changes the visible state to **Reduced motion · Cuts only**; the durable order then exposes the exact seven-beat consequence, supports presentation-only skip and hands control to the next canonical council. Restart resumes Turn 4 plus the cuts-only preference. A final durable order then seals the four-decision chapter, disables issue input and cold-replays at save SHA-256 `03f581a9f97de20d39ce07eddf314ed4ffeb396e9bebb435e9c3154e19fa3aee`. Native SDL output is also measured: direct PipeWire linkage captures exact digital silence for 4.01 seconds before opt-in and 3.97 seconds after disable, while the 16.13-second enabled stream measures −39.7 LUFS, −27.7 dBFS peak, 3.6 LU LRA, zero DC and exact channel parity. This is engineering evidence, not human listening approval.

Stable editor PIE is still red. The official editor rendered the real 33-actor command space and entered PIE, then Vulkan reported `AcquireNextImage() failed due to the outdated swapchain` and crashed in NVIDIA userspace (`libnvidia-glcore.so.595.84`). Physical-controller feel is also red because the workstation currently exposes no joystick or USB game controller. Full natural-motion comfort/lens review, human native-audio listening, reviewed final assets, film quality and observed human playability remain open rather than inferred from package stability.

Host audit: Ubuntu 24.04, 125 GiB RAM, 28 logical CPUs, two 24 GiB RTX 4090 D GPUs, NVIDIA 595.84 userspace and sufficient free home storage. Epic's current Unreal 5.8 Linux requirements recommend Ubuntu 22.04 or Rocky Linux 8, 32 GiB RAM, 8 GiB VRAM, NVIDIA 570+ and the versioned clang toolchain. Hardware exceeds the baseline. The Ubuntu-version difference and editor-only NVIDIA swapchain failure remain compatibility risks, but Linux packaging and normal-thread packaged performance are now directly measured green.

Resolution path:

1. Revise and recapture the Gate 5A release until the visible hand mesh—not only its wrist marker—shows contact and clearance legibly in both normal and reduced motion; exercise the fail-closed return visibly and retain v1–v3 plus normal-v4 only as diagnostics.
2. Resolve the Keeper-to-Chen player-ownership handoff and obtain independent hand-anatomy/acting, cinematic, historical-material, cultural-performance, accessibility and localization review; the current offscreen pass is only an authored assumption. Keep final prop/material, close framing, mouth interior, voice/lip sync and final character art as separate red gates.
3. Repeat the engagement campaign-byte guard after deliberate in-memory drift when a bounded runtime hook exists, preserving the last accepted save.
4. Connect a physical Xbox/PlayStation-layout controller and complete the remaining council/site/signal/evidence route plus natural consequences. Review exact deltas/text, speaker provenance, highlights, lenses, isolation, collision, silhouettes, eyelines, lighting and comfort.
5. Perform human listening review of the measured native audio path for rapid cues, ambience fatigue, stop/resume and device behavior; numeric capture alone is insufficient.
6. Preserve the accepted facial/skin baseline while closing its remaining gates: visibly inject and verify fail-closed fallback, replace the generic face and gray mouth with reviewed final character art and mouth interior, add reviewed voice/lip sync only through its separate language gate, and repeat close-framing, human art/historical, physical visual, performance and observed-player review. Then extend the accepted command-surface/wet-field/shelter/rain/vegetation blockouts with period-reviewed formations and lighting/weather response.

### Unity Editor import/build

The workstation is not authenticated to Unity. On 2026-08-08, Unity CLI installs for available 6000.x releases and direct official archive links redirected to `download.unitychina.cn` paths that returned HTTP 404. The Hub-visible 2022 LTS compatibility build did download from the regional service; its interrupted main transfer was reconstructed from exact HTTP ranges, checked for byte count and XZ integrity, installed, and detected automatically by Hub with Linux and Web modules.

The compatibility executable reports `2022.3.62f3c1` and its licensing client launches successfully. A refreshed 2026-08-09 batch preflight against an ignored compatibility copy again exits with code 1 before project import because no access token, license file or entitlement is available and Unity reports “No valid Unity Editor license found.” Hub's visible license panel says sign-in is required and that no licenses are present. A re-audit also found the former documented VNC/noVNC pair attached to an unrelated older X display; the new `5934`/`6134` pair was verified against Unity Hub on `:123` without disrupting that desktop. A private `.alf` request exists only under ignored `.runtime/`. No credentials, machine identifiers, tokens or license files are committed. The pinned Unity project has therefore **not** passed editor import, C# compilation, test runner, or player build. That gate stays red.

As of 2026-08-09, the expanded runtime, editor and EditMode-test sources—including deliberate select-only order cards, a complete selected-order reading, separate confirmation and eleven-locale decision labels, wartable marker picking, inspected-site state, localized intelligence UI, shared audio contract loading/validation, zero-mean deterministic rain, shared cue envelopes, peak/loudness/DC/loop/channel quality bounds, localized mixer UI, three carried player commitments with nine outcomes, three Qin pursuit postures, three strategic methods, neutral/unique-leader method-read selection, exact hit/miss counterplay, six-layer resolution, save-v6 identity checks, v1–v5 legacy replay and site closure validation—compile as separate assemblies with warnings treated as errors against the installed Unity 2022.3/Newtonsoft/NUnit reference set. Unity preflight independently reconstructs the raw rain and rejects excessive DC offset or a loop-boundary transient outside the shared limit; EditMode source tests also calculate cue peaks and commitment/pursuit/method/save behavior. The deterministic FFmpeg reference and actual-Chrome measurements prove the common authored contract and web path, not Unity's native mixer/output. This remains an offline source-type check, not an Editor import, audio-device review or test run, and does not change the red gate above.

Visible evidence:

- [`unity-01-editor-installed.png`](evidence/unity-01-editor-installed.png): Hub detects the editor with Linux and WebGL modules.
- [`unity-02-license-signin-required.png`](evidence/unity-02-license-signin-required.png): Hub requires account login to manage licenses.
- [`unity-engine-status.json`](evidence/unity-engine-status.json): machine-readable versions, module checks, archive integrity and the exact red gate; it contains no credentials or license material.

Resolution path:

1. Use the visible dedicated desktop to sign in to Unity Hub and activate an eligible Unity license. This is an account-owned action and cannot be fabricated by the build agent.
2. Re-run the compatibility-copy preflight to obtain a compiler result without allowing an older editor to rewrite the Unity 6 project.
3. Install the exact pinned editor plus Linux and Web build support when its official regional object becomes available.
4. Open `apps/unity`, allow package import, and fix migrations intentionally.
5. Run `./scripts/unity-pipeline.sh preflight`, `test`, `linux`, and `web`; then play the Boot scene and record artifact hashes/screenshots here.

### NVIDIA

The kernel/userspace update is now aligned at NVIDIA `595.84`; `nvidia-smi` succeeds and reports both RTX 4090 D GPUs, and official Unreal Vulkan initialized a real GPU. This resolves the earlier NVML mismatch. The normal-thread packaged player is stable and fast on the real 60 Hz display, but the editor-PIE presentation failure remains: PIE crashed in NVIDIA userspace after an outdated-swapchain acquire. Chrome WebGL was blocklisted in the first Web-visible test, whose accepted route therefore still uses ANGLE/SwiftShader. Blender CPU rendering is unaffected.

Do not reboot or reload production GPU modules without an explicit maintenance window. Packaged Unreal normal-thread Vulkan is accepted only for the measured fixed-window path; re-test full-screen, editor PIE and future high-fidelity asset loads separately rather than generalizing from this result.

## Unreal and Unity coexistence

Both project boundaries consume one canonical payload. New cinematic/3D feature work goes to Unreal; Unity remains buildable reference code while its account license gate is unresolved. The Web TypeScript engine remains the behavioral oracle until cross-engine replay fixtures prove Unreal and Unity parity. Story, effects, chronology and historical classification may never fork between clients.
